#include "cmdscreen/CSKeyMsg.h"

/*******************************************************************************
************************************************************************* types
********************************************************************************
 basic
*******************************************************************************/

static inline void cleanup( void* instance ) {}

cMeta const CS_KeyMsg = {
   .desc = stringify_c_( CS_KeyMsg ),
   .cleanup = &cleanup
};

/*******************************************************************************
********************************************************************* Functions
********************************************************************************

*******************************************************************************/

bool write_key_msg_cs( cRecorder rec[static 1],
                       CSKeyMsg const* msg,
                       char const fmt[static 1] )
{
   must_exist_c_( msg );

   return write_c_( rec, "key msg ",
                    "=> dec:{u16}", msg->cmd,
                    " / hex:{u16:x}", msg->cmd,
                    " / rune:{r}", msg->rune,
                    " / rune code:{r:n}", msg->rune );
}
